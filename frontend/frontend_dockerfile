# Stage 1: Build
FROM node:20-alpine
WORKDIR /app

# Install dependencies first (cached if package.json unchanged)
COPY package*.json ./
RUN npm install

# Copy the rest of the code
COPY . .

WORKDIR /app

EXPOSE 3000
CMD ["npm", "start"]